# 🚀 INFO TAWJIH 2.0 - Production Environment Variables for Netlify
# Copy these values and replace with your actual production values

# === DATABASE CONFIGURATION ===
# Replace with your actual Neon PostgreSQL connection string
DATABASE_URL=postgresql://username:password@ep-hostname.region.neon.tech/dbname?sslmode=require
DIRECT_DATABASE_URL=postgresql://username:password@ep-hostname.region.neon.tech/dbname?sslmode=require

# === AUTHENTICATION SECRETS ===
# Generate strong random secrets (32+ characters)
JWT_SECRET=your-very-strong-random-jwt-secret-32-chars-minimum-here
NEXTAUTH_SECRET=your-different-nextauth-secret-also-32-chars-minimum

# === DEPLOYMENT URLS ===
# Replace 'your-app-name' with your actual Netlify app name
NEXTAUTH_URL=https://your-app-name.netlify.app
ALLOWED_ORIGINS=https://your-app-name.netlify.app
SOCKET_ORIGINS=https://your-app-name.netlify.app

# === ADMIN CREDENTIALS ===
# Change these to your secure admin credentials
ADMIN_USERNAME=admin_tawjih
ADMIN_EMAIL=admin@tawjih-info.ma
ADMIN_PASSWORD=SecureAdminPassword123!

# === PRODUCTION ENVIRONMENT ===
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# === OPTIONAL: RATE LIMITING ===
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# === INSTRUCTIONS ===
# 1. Replace all placeholder values with your actual production values
# 2. Generate strong random secrets for JWT_SECRET and NEXTAUTH_SECRET
# 3. Use your actual Neon PostgreSQL connection string for DATABASE_URL
# 4. Update URLs to match your Netlify app domain
# 5. Set secure admin credentials
# 6. Upload this file to Netlify using "Import from a .env file"